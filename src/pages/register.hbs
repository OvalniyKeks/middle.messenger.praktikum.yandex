<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>Регистрация</title>
    <link rel="stylesheet" href="../sass/app.sass"/>
</head>
<body>
    <main class="page register flex flex-center">
        <div class="card">
            <form class="form form-register">
                {{> form/formTitle title="Регистрация"}}
                {{> input/input type="email" name="email" placeholder="Email "}}
                {{> input/input type="text" name="login" placeholder="Логин "}}
                {{> input/input type="text" name="first_name" placeholder="Имя "}}
                {{> input/input type="text" name="second_name" placeholder="Фамилия "}}
                {{> input/input type="tel" name="phone" placeholder="Телефон "}}

                {{> input/input type="password" name="password" placeholder="Пароль "}}
                {{> input/input type="password" name="password" placeholder="Повторите пароль "}}
                {{> button/button type="submit" link="./index.hbs" label="Зарегистрироваться"}}
                {{> form/formBottom textLabel="Уже есть аккаунт?" link='./login.hbs' linkLabel="Войти"}}
            </form>
        </div>
    </main>
</body>
</html>
